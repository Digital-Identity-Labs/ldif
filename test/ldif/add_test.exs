defmodule AddTest do
  use ExUnit.Case

  alias LDIF.Add

#  describe "new/2" do
#    test "returns struct" do
#      assert "cccc" = Add.new("", %{})
#    end
#  end
#
#  describe "apply/2" do
#    test "returns struct" do
#      assert "cccc" = Add.apply(change, entry)
#    end
#  end

end